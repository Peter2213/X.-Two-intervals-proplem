#include<iostream>
using namespace std;

int main()
{
	long long a, b, c, d;
	cin >> a >> b >> c >> d; 
	if (a < 1) {
		cout << -1;
	}

	else if (c < a && d < a || c > b && d > b) {
		cout << -1;
	}
	else if (c >= a && d >= b) {

		cout << c << " " << b;
	}
	else if (c <= a && d >= b) {
		cout << a << " " << b;
	}

	else if (a >= c && b >= d) {

		cout << a << " " << d;
	}
	else if (a == c && b == d) {
		cout << a << " " << b;
	}
	else if (c >= a && d <= b) {
		if (c > d) {
			cout << -1;
		}
		else {
			cout << c << " " << d;
		}
	}

	
	return 0;
}
